# Computational-statistics notebooks

The source collection had two broad notebooks: computational inference and time series/Bayes/MLE. The portfolio edition decomposes Monte Carlo, bootstrap, hypothesis testing, Bayesian updating and likelihood estimation into independent modules while retaining the time-series implementation already present in `src/`.

`computational_statistics_workbench.ipynb` provides the runnable integration path.
